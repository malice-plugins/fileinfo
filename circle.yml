machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    - docker build -t malice_fileinfo .
    - mkdir -p ~/docker; docker save malice_fileinfo > ~/docker/image.tar

test:
  override:
    - docker images malice_fileinfo    
    - docker run malice_fileinfo -t /bin/cat

deployment:
  # master:
  #   branch: master
  #   commands:
  #     - eval $(docker run gliderlabs/pagebuilder circleci-cmd)
  release:
    branch: release
    commands:
      - make release
